tests:
   - test:
      name: install ceph pre-requisites
      module: install_prereq.py
      abort-on-fail: true


   - test:
      name: cephadm initial cluster bootstrap
      module: test_cephadm.py
      desc: minimal ceph cluster using cephadm
      destroy-cluster: False
      abort-on-fail: true
